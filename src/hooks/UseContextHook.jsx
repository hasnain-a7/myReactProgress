/*in react we only data flow way is unidirectional and there is a problem in it which is prop 
drilling and to solve it we have context api which works as a globel state that any child directly
access insted of pass it component to component */

import React from "react";

const UseContextHook = () => {
  return <div></div>;
};

export default UseContextHook;
